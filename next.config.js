/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/taiwan_radio_lp',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

module.exports = nextConfig 